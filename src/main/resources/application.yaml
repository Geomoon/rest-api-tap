spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_tap_rest?useSSL=false&serverTimezone=America/Guayaquil
    username: root
    password: rootmysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: update

cloud:
  aws:
    region:
      static: us-east-1
    stack:
      auto: false
    credentials:
      access-key: AKIAQBD6KVJFFBETHNZX
      secret-key: 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

logging:
  level:
    org:
      hibernate:
        SQL: debug
